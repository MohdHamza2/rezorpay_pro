# Project State

**Current Phase:** Phase 6: Reporting & Dashboard (Wave 29)
**Status:** Wave 29 frontend implemented (AR/AP aging, VAT compliance, Dashboard enhancements) — ready to commit

## Recent Actions
- Wave 27 (WhatsApp Business API + PDF delivery + inbound→Enquiry): **addendum LOCKED** — `architecture/wave-whatsapp-pdf-addendum.md` rev 3. Peer review "APPROVE WITH REQUIRED CHANGES" → all 18 rev-1 corrective locks applied (rev 2), then final 3 mandates (rev 3): `received_at` + formal inbound/outbound timestamp semantics (INBOUND=RECEIVED, never SENT); `message_type=MEDIA` = inbound media/non-text transport record only — zero media download/inspection/storage/processing; exact idempotency reservation (`whatsapp_idempotency_keys` PK (workspace_id,key) + `request_fingerprint=sha256(canonical_json)`; same key+same request → existing result; same key+different request → 409 `IDEMPOTENCY_CONFLICT`; provider called ≤ once per key). Other locks: one WhatsApp phone/account per deployment (single inbound workspace via `WHATSAPP_INBOUND_WORKSPACE_ID`, deployment-level not multi-tenant routing); 3 PG enums (direction/status incl. RECEIVED/type) + global unique `whatsapp_message_id`; `DocumentRenderer` (INVOICE/QUOTATION/AR_STATEMENT, `build_sections` no-recompute seam, EN-only — bilingual server rendering deferred); `WhatsappProvider` in `providers.py` + dry-run `simulated`; normalized error triples (never raw provider bodies); `utils/phones.py` canonical E.164 (no country-code guessing, bare local → NULL, never guess-match); document recipient DERIVED from linked Client's canonical phone (no `to_number` override); lifecycle gates from live enums (INVOICE SENT/PARTIALLY_PAID/PAID/OVERDUE; QUOTATION SENT/ACCEPTED/CONVERTED); two-transaction outbound (provider call OUTSIDE a DB session), QUEUED = interrupted synchronous send (resend-recordable, not a queue); webhook GET challenge + POST HMAC + `metadata.phone_number_id` verification + body-size cap + response-precision matrix; inbound→Enquiry single transaction via EnquiryService session-injection (no self-commit), DB unique constraints are the dedup authority (race → idempotent 200). `ErrorCode.UNSUPPORTED_DOCUMENT` + `IDEMPOTENCY_CONFLICT`. Meta Graph API version + reportlab version = verify-at-implementation acceptance gates (not guessed pins). **No Wave 27 code yet** — implementation begins report-first.
- Wave 26 (Email Engine via Resend): implemented end-to-end per `architecture/wave-email-engine-addendum.md` (its 12 parent planning locks already committed `3983160`). **Alembic moves HEAD**: new migration `3d3f24d6ee00_add_email_log` (down_revision `234d5639ef8c`; `email_log` + `email_idempotency_keys` + `emailstatus` PG enum QUEUED|SENT|DELIVERED|BOUNCED|FAILED; composite indexes `ix_email_log_workspace_id_created_at` / `ix_email_log_workspace_linked`; unique `uq_email_log_resend_message_id`); round-trip + `alembic check` clean. `EmailLog`/`EmailIdempotencyKey`/`EmailStatus` exported. `providers.py` async adapter layer (`ProviderResult` + `ResendProvider` via httpx) with `COMMS_DRY_RUN` default true (simulated marker, no network in CI). `email_service.py`: send/list/get/resend/webhook; Idempotency-Key (48h); linked-entity resolver (AR_STATEMENT/SUPPLIER_STATEMENT **reference-only, no physical FK**); provider failure commits FAILED then 502 `PROVIDER_ERROR` (resend-capable); never logs secrets/PII/bodies. Router `comms_emails.py` wired at `/api/v1`: POST `/comms/emails` (OWNER/ADMIN, 30/min), GET list (member, workspace-scoped paginated), GET `{id}`, POST `{id}/resend` (FAILED/QUEUED only), POST `/webhooks/email` (Bearer `RESEND_WEBHOOK_SECRET`, no limiter). Config `RESEND_API_KEY`/`RESEND_FROM_EMAIL`/`RESEND_WEBHOOK_SECRET`/`COMMS_DRY_RUN` (+ `.env.example`). Backend suite **333 passed** (322 + 11 new in `test_email_comms.py`); ruff + black clean on touched files; `alembic check` clean. Guardian pins re-pointed to `3d3f24d6ee00`. Pre-existing black drift in `tests/test_enquiries.py`/`tests/test_ar_statement.py` noted (unrelated, excluded from commit).
- Wave 25 (AP payment reversal — SUCCESS CHEQUE/CASH/BANK -> FAILED after a bank bounce): closes the last Phase 4 leftover per `architecture/wave-ap-payment-reversal-addendum.md`. **No Alembic** (HEAD stays `234d5639ef8c`; `alembic check` clean; no new columns/enums). New `supplier_payment_reversal_service.py` (`SupplierPaymentReversalService`, dedicated file so `supplier_payment_service.py` stays <500 — Wave 24 split valve): `reverse_payment` FOR UPDATE invoice+payment, OWNER/ADMIN only (MEMBER 403 `INSUFFICIENT_PERMISSIONS`), non-PDC only (PDC CLEARED/PENDING/BOUNCED/RETURNED -> 403 `INVALID_STATE`; a CLEARED PDC is terminal, mirror AR), SUCCESS-only (already-FAILED -> idempotent 200), open-invoice guard (APPROVED/PARTIALLY_PAID/PAID; CANCELLED -> 403), defensive amount<=amount_paid 400. Effect: `status -> FAILED` + `_reverse_settlement` (exact inverse of `_settle_invoice`: `amount_paid -= amount; balance_due += amount`; PAID stepped back to PARTIALLY_PAID then APPROVED when paid 0; `paid_at` cleared). Statement/aging/ap-balance read live stored columns + omit FAILED (existing `PAYMENT_STATUSES`) so a reversed payment re-enters open AP and `closing_running == amount_due_now` identity holds — **zero statement/aging code change**. Router: `POST /supplier-invoices/{id}/payments/{id}/reverse` (10/minute, empty `{}` body `SupplierPaymentReversalRequest` extra=forbid, no Idempotency-Key). Backend suite **322 passed** (314 + 8 new in `test_supplier_payment_reversal.py`); ruff + black clean; `alembic check` clean.
- Wave 24 (PDC-to-supplier, AP post-dated cheque issued): implemented end-to-end per `architecture/wave-pdc-to-supplier-addendum.md` — generalizes the AR PDC machine (RECEIVED→DEPOSITED→CLEARED|BOUNCED, RECEIVED→RETURNED) to the payable side. `SupplierPayment` gains `pdc_date`/`pdc_status` (columns via alembic `234d5639ef8c`, down_revision `b4a2c6e8f10d`, round-trip + `alembic check` clean, new head; **no** new PG types — `paymentmethod`/`paymentstatus`/`pdcstatus` reused). `record_payment`: `payment_method=PDC` → `PENDING`+`RECEIVED`, `pdc_date` required (422 `VALIDATION_ERROR` field=pdc_date at service, mirror AR), **skips** `_settle_invoice` (balance_due unchanged, invoice stays APPROVED); CASH/BANK_TRANSFER/CHEQUE stay SUCCESS-on-receipt. New `supplier_pdc_service.py` (mirrors `pdc_service.py`): FOR UPDATE invoice+payment, 404 cross-workspace, OVER-clear 400 `PAYMENT_EXCEEDS_BALANCE`, clear reuses `_settle_invoice(amount)` incrementally (DN-credit safe), bounce FAILED / return CANCELLED, idempotent target-state 200s, PUT 405 unchanged. Router: four `POST /supplier-invoices/{id}/payments/{id}/pdc/{deposit|clear|bounce|return}` (OWNER/ADMIN → MEMBER 403 `INSUFFICIENT_PERMISSIONS`, 10/minute, empty `{}` body, no Idempotency-Key). Statement/aging unchanged (PENDING already renders `SUPPLIER_PAYMENT_PENDING`, not in paid; FAILED/CANCELLED omitted). Backend suite **314 passed** (299 + 15 new in `test_supplier_pdc.py`); ruff + black clean; `alembic check` clean. Guardian pins `test_pdc.py`/`test_pricing.py` re-pointed to `234d5639ef8c` — Wave 25 keeps them (no head change).
- Wave 23 (Purchase Returns + Supplier Debit Notes, AP): implemented end-to-end per `architecture/wave-purchase-returns-addendum.md` — models (`PurchaseReturn` + `PurchaseReturnItem` + `PurchaseReturnCounter` + `ReturnType`/`PurchaseReturnStatus` QUALITY_ISSUE/DAMAGE/EXCESS + DRAFT→PENDING_SUPPLIER→APPROVED→DISPATCHED→COMPLETED; `SupplierDebitNote` + `SupplierDebitNoteCounter` + `SupplierDebitNoteStatus` DRAFT/ISSUED/APPLIED/CANCELLED; `nullable=False` on prn_number/dn_number), alembic `b4a2c6e8f10d` (5 tables + 3 enums + 4 checks; downgrade/upgrade round-trip + `alembic check` clean, head), gapless PRN/SDN numbering, services (`purchase_return_service` full state machine with received-cap invariant + exclude_return_id cap fix + `_group_quantities`, dispatch stock-out `min(quantity, remaining_accepted, on_hand)` via FOR UPDATE bin + PRN ISSUE ledger rows only when stock actually moves; `supplier_debit_note_service` single-txn apply reducing only `balance_due` — never amount_paid/status/paid_at; issue-dispatch auto-SDN), GRN-004 disposition auto-return hook (`record_disposition_auto_items`, rejected→QUALITY_ISSUE / damaged→DAMAGE at `spo_item.unit_price`, accumulate, cap ≤ quantity_received), supplier statement DN integration (credit line at applied_at, opening reconstruction subtracts pre-window credits, `totals.credited`, ACTIVITY_ORDER invoice<DN<payment), routers `/api/v1/purchase-returns` + `/api/v1/supplier-debit-notes` wired (191 routes). Also fixed `supplier_payment_service._settle_invoice` to reduce `balance_due -= amount` incrementally (was `total - amount_paid`, erasing DN credits). Backend suite **299 passed** (279 + 21 new tests across `test_purchase_returns`/`test_supplier_debit_notes`/`test_supplier_statement_dn`); ruff + black clean; `alembic check` clean; committed `e256772`. Guardian pins `test_pdc.py`/`test_pricing.py` re-pointed to `b4a2c6e8f10d`; `test_ap_aging.py` made UTC-robust (UTC-date flake at this machine's tz).
- Wave 22 (Supplier AP Payments & Aging): implemented end-to-end — models (`SupplierPayment` + `SupplierPaymentIdempotencyKey` mirroring AR `Payment`/`IdempotencyKey`, workspace-scoped composite PK + 48h TTL, `CheckConstraint amount > 0`, reuses `paymentmethod`/`paymentstatus` enums), alembic `a3f4c7d9e1b2` (`supplier_payments` + `supplier_payment_idempotency_keys` + 5 indexes; downgrade/upgrade round-trip + `alembic check` verified), schemas (`SupplierPaymentCreate` restricted to CASH/BANK_TRANSFER/CHEQUE with no-future `payment_date`; `ap_aging` summary/detail/by_supplier incl. `_AgingRow` DateTime→Date adapter onto the live AR `aging_buckets` + bucket keys `current/days_1_30/days_31_60/days_61_90/days_90_plus`; `supplier_statements` doc-type enum + statement JSON with `from` alias), services (`supplier_payment_service.record_payment` — FOR UPDATE row lock, in-transaction workspace-scoped idempotency check (48h), eligibility gate only APPROVED/PARTIALLY_PAID else 400 `INVALID_STATE`, no overpayment else 400 `PAYMENT_EXCEEDS_BALANCE`, inserts immutable `SUCCESS` payment + idempotency key, `_settle_invoice` writes stored `amount_paid`/`balance_due`/`paid_at` and transitions APPROVED→PARTIALLY_PAID→PAID; `ap_aging` summary/detail/by_supplier; `supplier_statement_service.get_statement` generated AP ledger reusing AR `validate_statement_dates` + `assert_activity_cap`, opening reconstruction = Σ pre-period invoice totals − Σ pre-period SUCCESS payments, activity lines w/ running balances, totals, `amount_due_now` + aging footer), router `supplier_payments.py` wired into `/api/v1` (POST /supplier-payments Idempotency-Key-required + `10/minute` rate limit, GET list w/ supplier/invoice/status/date filters + pagination, GET /{id}, PUT /{id}→405, GET /supplier-invoices/{id}/ap-balance, GET /supplier-invoices/{id}/payments, GET /ap-aging[/detail|/by-supplier], GET /suppliers/{id}/statement). Backend suite **279 passed** (255 baseline + 24 new AP tests across `test_supplier_payments`, `test_ap_aging`, `test_supplier_statement`); ruff + black clean; `alembic check` clean; committed. AP ledger line counts capped at 2000 (`STATEMENT_TOO_LARGE`) like AR.
- Executed Phase 2 (Wave 7 Enquiry Management).
- Verified Enquiry Management in backend (SQLModel, schemas, endpoints).
- Verified Enquiry Management via UI in browser subagent (Kanban rendering, Detail View, Client linking).
- Ran automated Playwright E2E tests, which successfully pass.
- Full audit 2026-09-06: fixed frontend production build (debit-note PDF/AR panel/enquiry TS errors), fixed backend test suite (228 passing), added frontend lint+build job to CI.
- Audit remediation (M-series): gapless PR/RFQ/GRN numbering (counters + FOR UPDATE, alembic `9f3a2c1e5d84`), SECRET_KEY production guard, configurable CORS_ORIGINS, health E402, deleted leftover root test scripts. Backend suite 228 passed incl. new concurrent PR/RFQ/GRN numbering tests; ruff + black clean.
- Wave 18 (Stock Reservations from Customer POs): implemented end-to-end — models (`StockReservation`, `StockReservationItem`, `ReservationStatus`), alembic `b2a4d6f8e1c0` (locked invariant: `InventoryLevel.reserved == SUM(quantity - quantity_consumed)` over ACTIVE lines), schemas, service (`create/cancel/expire/release_for_dispatch`, 7-day TTL, FIFO release), router endpoints (create 201, list with status/cpo/warehouse filters, get, cancel, owner/admin expire), DN confirm integration (release before `post_issue`). Backend suite 238 passed incl. 7 new reservation tests; ruff + black clean; `alembic check` clean.
- Wave 19 (Multi-warehouse Stock Transfers): implemented end-to-end — models (`StockTransfer`/`StockTransferItem`/`StockTransferCounter` + `TransferStatus`), alembic `c6b3e7a9d2f0` (adds `inventory_levels.in_transit` swing column + check `>= 0`, gapless `ST-YYYY-0001` counter), schemas (incl. partial-receive discrepancy via `received_quantity`), service (`create/approve/dispatch/receive/cancel`, state machine DRAFT→APPROVED→IN_TRANSIT→RECEIVED, `in_transit` swing accounting mirrored as TRANSFER ledger txns, cancel IN_TRANSIT returns to source), router endpoints (create 201, paginated list with status/source/dest filters, get, approve/dispatch/receive/cancel). Backend suite 246 passed incl. 8 new transfer tests; ruff + black clean; `alembic check` clean.
- Wave 19b (Stock Counting / Reconciliation): implemented end-to-end — models (`StockCount`/`StockCountItem`/`StockCountCounter` + `StockCountStatus` SCHEDULED→IN_PROGRESS→COMPLETED→RECONCILED, CANCELLED; unique (count_id, product_id, bin_id) line; tz-aware lifecycle timestamps), alembic `e1f5b8a2c3d4` (tables `stock_counts`/`stock_count_items`/`stock_count_counters`, gapless `SC-YYYY-0001` counter), schemas (`CountRecordRequest` extra=forbid + non-negative, `StockCountResponse` with variance), service (`create_count` snapshots `on_hand` per bin at schedule, `record/complete/approve/reconcile/cancel`; `COUNT_TOLERANCE_PERCENT=2.00` multiplication compare flags `requires_approval`; reconcile applies `on_hand += variance` via `lock_or_create_level` + writes ADJUSTMENT ledger rows `reason=COUNT_CORRECTION`; `in_transit`/`reserved`/`damaged` never touched), router endpoints (create 201, paginated list with status/warehouse filters, get, record/complete/approve/reconcile/cancel; OWNER/ADMIN-only mutations, GET open to members). Backend suite 255 passed incl. 9 new stock-count tests; ruff + black clean; `alembic check` clean; migration downgrade/upgrade round-trip verified. Restored three guard test modules after a PowerShell `python -c` truncation incident and re-pinned alembic head assertions to `e1f5b8a2c3d4` (`test_pdc`, `test_pricing`); `test_product_electrical_specs` rewritten to walk the head→root lineage dynamically so future waves never break it.

During the previous session, the supplier-invoice 3-way match engine (Phase 4 = "AP Payments & Aging"; Wave 21) was validated as complete: the supplier-invoice lifecycle reaches `approve`; `PARTIALLY_PAID`/`PAID` statuses and stored `amount_paid`/`balance_due`/`paid_at` columns already exist but nothing writes them — that hook is what Wave 22 fills.
- Wave 22 (Supplier AP Payments & Aging): implemented end-to-end — models (`SupplierPayment` + `SupplierPaymentIdempotencyKey` mirroring AR `Payment`/`IdempotencyKey`, workspace-scoped composite PK + 48h TTL, `CheckConstraint amount > 0`, reuses `paymentmethod`/`paymentstatus` enums), alembic `a3f4c7d9e1b2` (`supplier_payments` + `supplier_payment_idempotency_keys` + 5 indexes; downgrade/upgrade round-trip + `alembic check` verified), schemas (`SupplierPaymentCreate` restricted to CASH/BANK_TRANSFER/CHEQUE with no-future `payment_date`; `ap_aging` summary/detail/by_supplier incl. `_AgingRow` DateTime→Date adapter onto the live AR `aging_buckets` + bucket keys `current/days_1_30/days_31_60/days_61_90/days_90_plus`; `supplier_statements` doc-type enum + statement JSON with `from` alias), services (`supplier_payment_service.record_payment` — FOR UPDATE row lock, in-transaction workspace-scoped idempotency check (48h), eligibility gate only APPROVED/PARTIALLY_PAID else 400 `INVALID_STATE`, no overpayment else 400 `PAYMENT_EXCEEDS_BALANCE`, inserts immutable `SUCCESS` payment + idempotency key, `_settle_invoice` writes stored `amount_paid`/`balance_due`/`paid_at` and transitions APPROVED→PARTIALLY_PAID→PAID; `ap_aging` summary/detail/by_supplier; `supplier_statement_service.get_statement` generated AP ledger reusing AR `validate_statement_dates` + `assert_activity_cap`, opening reconstruction = Σ pre-period invoice totals − Σ pre-period SUCCESS payments, activity lines w/ running balances, totals, `amount_due_now` + aging footer), router `supplier_payments.py` wired into `/api/v1` (POST /supplier-payments Idempotency-Key-required + `10/minute` rate limit, GET list w/ supplier/invoice/status/date filters + pagination, GET /{id}, PUT /{id}→405, GET /supplier-invoices/{id}/ap-balance, GET /supplier-invoices/{id}/payments, GET /ap-aging[/detail|/by-supplier], GET /suppliers/{id}/statement). Backend suite **279 passed** (255 baseline + 24 new AP tests across `test_supplier_payments.py` / `test_ap_aging.py` / `test_supplier_statement.py`); ruff + black clean; guardian alembic-head pins re-pinned to `a3f4c7d9e1b2` in `test_pdc` / `test_pricing`.

## Pending Works
- [x] Research Advanced Inventory domain (stock reservations from Customer POs, multi-warehouse transfers, stock counting/reconciliation).
- [x] Create implementation plan for Phase 3 (Wave 18 reservations addendum written; research complete).
- [x] Wave 18: Stock Reservations from Customer POs (models, migration, service, API, DN dispatch integration, tests).
- [x] Wave 19: Multi-warehouse Stock Transfers.
- [x] Wave 19b: Stock Counting / Reconciliation.
- [x] Phase 4 (AP Payments & Aging): Wave 22 addendum written (`architecture/wave-ap-payments-addendum.md`); Wave 21 supplier 3-way match hook validated (ends at `approve`; paid-status columns untouched until Wave 22).
- [x] Wave 22: Supplier payment recording, AP ledger (generated supplier statement), supplier AP aging.
- [x] Wave 23: Purchase returns + supplier debit notes (AP returns/credits; GRN-004 auto-return; statement integration).
- [x] Wave 24: PDC-to-supplier (AP post-dated cheque lifecycle; `architecture/wave-pdc-to-supplier-addendum.md`).
- [x] Wave 25: AP payment reversal (SUCCESS CHEQUE/CASH/BANK -> FAILED after bank bounce; `architecture/wave-ap-payment-reversal-addendum.md`). Phase 4 is now complete — no leftovers.
- Phase 5 (Communications & Integrations): planning **approved after Review verdict v1** (12 architecture locks applied to `architecture/phase-5-comms-integrations-planning.md` — ROADMAP set as authoritative over MASTER_PLAN_V3 numbering; PDF `DocumentRenderer` formalized as the canonical server-side outbound-comms primitive (reconciles `wave-bilingual-pdf-addendum.md` client-side `@react-pdf`, no dual systems); WhatsApp inbound → Enquiry with `wa_message_id` dedup + unknown-contact `client_id` nullable (no LLM); dry-run default with `simulated` marker; `/send` decoupled; env-keyed creds, never log secrets/PII; VAT read-only export by business date, no invented VAT states, never exclude overdue-only; synchronous infra, manual resend only).
- [x] Wave 26: Email Engine via Resend (implemented + committed this session).
- [x] Wave 27: addendum LOCKED — `architecture/wave-whatsapp-pdf-addendum.md` rev 3 (review-passed; `DocumentRenderer` per phase §2.1). **Implementation begins next** (report-first; then commit per wave).
- [x] Wave 28: UAE VAT pack (read-only export by business date). **IMPLEMENTED + COMMITTED** — config `VAT_ORG_TRN`, `schemas/vat_compliance.py`, `services/vat_compliance_service.py` (business_date GST helper, AED-only input buckets, manifest warnings, csv_text, build_zip), `routers/reports.py` (`GET /api/v1/reports/vat-compliance`), `main.py` wired. **13 tests** passing (`tests/test_vat_compliance.py`): zip integrity, per-line math, summary netting (CN/TDN), excluded statuses, OVERDUE inclusion, period guards 422, MEMBER RBAC 403, JSON mirror, business-date-not-created_at, no-TRN tolerance, workspace isolation, non-AED supplier invoices (listed but never aggregated), GST business-date rendering. Full suite **397 passed**, ruff clean, alembic check clean (no Alembic).
- [x] Wave 29: Reporting & Dashboard (Phase 6). **Frontend implemented** — `frontend/src/api/reports.ts` (AR/AP aging types + fetchers, VAT blob/JSON fetchers, bucket labels, formatAed); `frontend/src/pages/Reports.tsx` + `Reports.module.css` (3-tab page: AR Aging, AP Aging, VAT Compliance; recharts bucket bar charts; lazy detail loading; VAT permission gating — MEMBER sees notice, no fetch fired; OWNER/ADMIN gets on-demand JSON preview + ZIP/JSON downloads); `Dashboard.tsx` enhanced (Recent Invoices widget wired to `GET /api/v1/invoices?page=1&per_page=5`; Open AR/AP snapshot from ar-aging + ap-aging summaries; Quick Actions wired to `/invoices`, `/procurement`, `/grn`); `Layout.tsx` sidebar nav adds Reports (`BarChart3` icon); `App.tsx` route `/reports` registered. `frontend/src/api/invoices.ts` gains `getRecentInvoices`. **npm run build** clean; **npx oxlint** clean (zero new warnings). Backend: AR aging endpoints implemented + 11 tests passing; full backend suite previously verified at 408 passed. No Alembic. Refer to `architecture/wave-reports-dashboard-addendum.md` (rev 2, locked).
