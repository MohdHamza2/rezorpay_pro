# WP-A Bilingual PDF — code review (font + labels + filesystem pytest)

**Date:** 2026-09-01
**Scope:** `architecture/wave-bilingual-pdf-addendum.md` WP-A + §2.1 titles + §3 font + §5 + §7; `.agents/reports/architect-bilingual-pdf-note.md`
**Review only.** No WP-B layout. No git commit.

## Verdict

**APPROVE_WITH_NITS**

No P0: no CDN/runtime font fetch, no user-upload font, no Alembic / `*_ar` columns, live EN PDF titles unchanged.

**WP-B may start.**

## P0s

None.

## Checklist

| # | Lock | Result |
|---|---|---|
| 1 | TTF exists, 50 KiB–2 MiB, TTF/OTF magic, OFL mentions SIL OPEN FONT LICENSE | **Pass.** `frontend/src/assets/fonts/NotoNaskhArabic-Regular.ttf` **247,336** bytes. Magic `\x00\x01\x00\x00` (TrueType). Name table UTF-16BE contains **Noto Naskh Arabic** (not Noto Sans). Hinted glyf font (`fpgm`/`prep`/`cvt `); no `fvar` (not variable). Regular only — fonts dir is TTF + `OFL.txt`. OFL.txt is SIL OFL 1.1 from notofonts/arabic and contains `SIL OPEN FONT LICENSE`. |
| 2 | pdfFonts: `Font.register`, `NotoNaskhArabic`, Vite `?url`, no googleapis/gstatic/`http://`, hyphenation `[word]`, Helvetica EN default | **Pass (source).** `pdfFonts.ts` imports `NotoNaskhArabic-Regular.ttf?url`, `Font.register({ family: 'NotoNaskhArabic', src: notoUrl })`, `Font.registerHyphenationCallback((word) => [word])`, `PDF_FONT_AR` / `PDF_FONT_EN = 'Helvetica'`. No `fonts.googleapis` / `fonts.gstatic` / `http://` / jsDelivr anywhere under `frontend/src`. Idempotent `registered` guard is extra vs the spec snippet (good). Pytest gates register/family/filename/CDN strings only — see nits. |
| 3 | `PDF_TITLES` six EN exact + six AR exact; LPO AR `أمر شراء محلي`; EN `LPO` not PO Acknowledgement | **Pass.** Module matches addendum §5.1 / §2.1 byte-for-byte. No `Purchase Order Acknowledgement`, no paper AR `إشعار أمر شراء`. |
| 4 | pdfLabels covers required chrome keys from §5.2 | **Pass (source).** Every §5.2 EN/AR pair is present (party chrome, meta, table headers, totals, statement type/aging, opening/payment pending). Aging ranges stay Western (`1–30` / `31–60` / `61–90` / `90+`, en-dash). Extra keys (watermarks §4.4, footer disclaimer §4.5, `Generated by InvoiceSaaS` EN-only, Tax Invoice / Tax Credit Note for `doc_type` display) are the right place for WP-B. Pytest does **not** open this file — nit. |
| 5 | No `*PDF.tsx` / preview retitling in this WP | **Pass.** Git: untracked only `pdfFonts.ts` / `pdfTitles.ts` / `pdfLabels.ts` + fonts + pytest. No diff on the six PDFs, six previews, or `ArStatement.tsx`. Live titles still **Tax Invoice**, **Tax Credit Note**, **Quotation**, **LPO**, **Delivery Note**, **Account Statement**. No `pdf-title-ar` siblings yet (WP-B). Page default remains `fontFamily: 'Helvetica'`. Playwright EN `toHaveText` still valid. |
| 6 | No `name_ar` / `address_ar` on workspace / client / product | **Pass.** Grep of `backend/app/models` and `backend/alembic` is empty. Models still `name` / `address` (English). |
| 7 | No new Alembic revision; HEAD `b8d5f0c3a216` | **Pass.** `alembic heads` → **`b8d5f0c3a216 (head)`**. No `down_revision = "b8d5f0c3a216"`. No `*_bilingual*` / `*_name_ar*` files. Credit-notes revision file still present. |
| 8 | Pytest §7 actually asserts the above | **Pass vs spec §7; partial vs coordinator extras.** Re-ran `tests/test_bilingual_pdf_assets.py`: **6 passed**, 0 failed (0.01s). Filesystem only; no DB/SQLite fixtures. Repo root = parent of `backend/`. Asserts: font size/magic, OFL phrase, six EN+AR substrings in `pdfTitles.ts`, `Font.register` + `NotoNaskhArabic` + TTF filename + no googleapis/gstatic/`http://`, models have no `name_ar`/`address_ar`, alembic versions still contain `b8d5f0c3a216_add_credit_notes.py` and no bilingual/name_ar revision names. Does not assert pdfLabels, `?url`, hyphenation, Helvetica, or `alembic heads`. |

## Nits

1. **`pdfLabels.ts` has no pytest.** Addendum §7 omits it; coordinator checklist 4 therefore has no CI gate. WP-B could drop chrome AR and this file would still be green until Playwright.
2. **`test_pdf_fonts_register_local_ttf_not_cdn` is string-contains only.** It does not require `?url`, `(word) => [word]`, or `PDF_FONT_EN = 'Helvetica'`. Source has all three. Also does not mention jsDelivr (spec §3.1); source is clean.
3. **Alembic test is filename-pattern, not HEAD.** A new revision not named `*_bilingual*` / `*_name_ar*` would pass. Reviewer confirmed live HEAD is still `b8d5f0c3a216`.
4. **Title test is substring `in text`**, not `en: '…'` field match. `pdfTitles.ts` is eight lines, so it is equivalent today.
5. **WP-B mapping (not a WP-A defect):** LPO PDF live column header is `VAT`, labels have `VAT AED`. `ArStatement.tsx` page copy uses `Billed` / `Paid (SUCCESS payments)` without the PDF/spec colons; `StatementPDF` already matches spec (`Billed:`, `Paid (SUCCESS):`). Keep EN testids on EN-only nodes.

Not P0 / not blocking WP-B: font is a real hinted Noto Naskh Arabic Regular in-repo; titles are the locked six; live Playwright EN strings were not touched; no schema.

## WP-B

**Yes — WP-B may start** on this tree.

Import `registerPdfFonts()` from every `*PDF.tsx` and each `pdf().toBlob()` / download path (invoice download lives in `Invoices.tsx`). Dual title from `PDF_TITLES`; stacked chrome from `PDF_LABELS`; AR `Text` uses `PDF_FONT_AR` with **no** `fontWeight: 'bold'`. Do not `direction: 'rtl'` the page. Do not retitle EN (`LPO` stays `LPO`). Put AR on sibling `*-ar` testids so existing `toHaveText` stays exact. Same TTF via CSS `@font-face` for HTML preview. No Alembic, no `*_ar` columns, no CDN font, no server PDF route.
